# Prompt 06 — Finalize API Conventions

```text
Read accepted source-of-truth documents.
Review docs/architecture/API_CONVENTIONS.md and extend it with endpoint naming, request IDs, idempotency keys, cursor format, error codes, validation responses, optimistic concurrency, and ownership-failure behaviour.

Create an endpoint inventory for MVP modules without implementing controllers.
Keep real users fully isolated.
Do not expose EF entities.
Do not write code.
```
