# Parallel World mobile shell

This Android-first Flutter application is pinned to Flutter 3.47.1 with bundled Dart 3.13.1. It intentionally contains no product features during M01.

From this directory, run `flutter pub get`, `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, and `flutter test`.
