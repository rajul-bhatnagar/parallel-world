# Docker development

This directory is reserved for local Docker development assets. M01 intentionally does not add Docker Compose, PostgreSQL, migrations, or a backend container; those become applicable in later milestones.
